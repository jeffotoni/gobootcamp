name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Deploy static site to S3 bucket
      run: aws s3 cp ./gomanual/manual/ s3://${{ secrets.S3_BUCKET }}/ --recursive

# name: Test Run
# on:
#   push
# jobs:
#   lambda:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@master
#       -  run: echo "<html><head><title>GH Actions Test</title></head><body><h1>YO THIS WORKS!</h1></body></html>" > jeffotoni.html
#       - uses: stcalica/s3-upload@master
#         with:
#           bucket: ${{ secrets.S3_BUCKET }}
#           package: jeffotoni.html
#           key: jeffotoni
#           AWS_REGION: ${{ secrets.AWS_REGION }}
#           AWS_SECRET_ID: ${{ secrets.AWS_SECRET_ID }}
#           AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}